const config = {};

config.mongodb = {};

config.mongodb.userName = "username"; // replace with username and password for your database connection
config.mongodb.password = "password";

module.exports = config;
